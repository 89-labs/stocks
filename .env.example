# MongoDB (Atlas free tier works)
MONGODB_URI="mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority"
MONGODB_DB="naijastocks"

# NextAuth — required for sign-in / sign-out
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
# Email/password accounts (requires MongoDB)
# MONGODB_URI must be set — see above

# Google OAuth (optional — sign up / sign in with Google)
# Create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email magic link (optional)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_FROM="noreply@naijastocks.com"

# Upstash Redis (optional) — serverless HTTP caching + rate limiting
# Create a database at https://console.upstash.com and copy REST credentials
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# NGX live board prices (primary quote source) — https://ngxpulse.ng/api
NGX_PULSE_API_KEY=""
# Refresh gate: max ~72 calls/day at 20 min; hard cap 80/day
# NGX_PULSE_REFRESH_MINUTES=20
# NGX_PULSE_DAILY_MAX_REQUESTS=80

# iTick API — get your token at https://itick.org/dashboard
ITICK_API_KEY=
# Free tier (default): https://api-free.itick.org — paid: https://api.itick.org
ITICK_BASE_URL=https://api-free.itick.org
# Optional: free-tier rate limit tuning
# ITICK_LIVE_QUOTES_LIMIT=30
# ITICK_QUOTES_BATCH_SIZE=5
# ITICK_QUOTES_BATCH_DELAY_MS=600
# ITICK_KLINE_DELAY_MS=550

# Optional: NGN/USD via ExchangeRate-API
EXCHANGE_RATE_API_KEY=""

# Groq — LLM provider for the Mastra agent layer (https://console.groq.com)
GROQ_API_KEY=""

# Mastra persistence (libSQL) — local SQLite file by default.
# For Turso/remote, use: libsql://<db>.turso.io and set MASTRA_DB_AUTH_TOKEN.
MASTRA_DB_URL="file:./mastra.db"
# MASTRA_DB_AUTH_TOKEN=""

# Vercel Cron — secures GET /api/news/refresh (news re-ingest every 3 hours)
CRON_SECRET="generate-with: openssl rand -base64 32"
